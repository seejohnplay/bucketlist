<h2>Bucket Lists</h2>

<table class="table">
	<tbody>
	<% @lists.each do |list| %>
		<tr>
			<td>
				<%= link_to list.title, list_path(list) %>
			</td>
		</tr>
	</tbody>
	<% end %>
</table>

<%= link_to 'Create new list', new_list_path, :class => 'btn btn-primary' %>